[defaults]
host_key_checking = False
inventory = hosts
retry_files_enabled = False
# stdout_callback = yaml # Disabled in favor of the new built-in plugin with the same purpose
bin_ansible_callbacks = True
roles_path = ./roles
